/**
 * @format
 */

import React from 'react';

describe('App Tests', () => {
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should have React imported', () => {
    expect(React).toBeDefined();
  });
});
